一块尺寸为1m×1m的弹性板，正方形区域的中间添加一个半径为 0.2 米的圆形孔洞，其杨氏模量为1GP，泊松比为0.3。
它的左边缘位移为零，右边缘沿y方向的位移为 0.1 米。
网格细化为50×50的单元，使用 FENICS 求解位移，并将位移结果存储为 PNG 文件。

如图为正方形弹性板，边长在图中标注，中心半径为 0.2 米的圆形孔洞，其杨氏模量为1GP，泊松比为0.3。
它的左边缘位移为零，右边缘沿y方向的位移为 0.1 米。
网格细化为50×50的单元，使用 FENICS 求解位移，并将位移结果存储为 PNG 文件。