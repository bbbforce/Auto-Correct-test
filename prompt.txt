在一个单位正方形上模拟 2D 稳态热传导。
左侧边界恒定为 100 度，其余三个边界绝热。
请确保生成完整的可以通过执行的FEniCS python代码。每次运行必须在代码所在目录的 result/ 文件夹下根据当前时间（例如使用 datetime 模块）创建一个新的子文件夹（例如 result/run_20260313_163000/），并且必须将所有的计算结果输出文件（如 .pvd, .vtu, .xdmf 等）保存到该新建的子文件夹中。
